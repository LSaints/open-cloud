version: '3.8'

services:
  db:
    image: postgres:15 
    container_name: postgres_db
    environment:
      POSTGRES_DB: opencloud         
      POSTGRES_USER: opencloud          
      POSTGRES_PASSWORD: opencloud@db  
    ports:
      - "5432:5432"  